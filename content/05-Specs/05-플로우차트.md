# LifeSpeech 플로우차트

#spec #flow #lifespeech

**작성일:** 2026-02-11  
**버전:** 1.0

---

## 1. 전체 업무 플로우 (운영앱 플로우 기반)

```mermaid
flowchart TD
    A[네이버 예약] -->|실시간/주기| B[운영 앱 수집]
    B --> C{방문 체크}
    C -->|미방문| D[리마인드 문자]
    C -->|방문| E[현장 기입+DB 구축]
    E --> F{등록 여부}
    F -->|그룹반 등록| G[자동 문자/카톡]
    G --> H[전자계약서 발송]
    H --> I[출석부 7회차 자동 등록]
    I --> J[회차별 문자]
    F -->|1:1 등록| K[구글 스케줄 매칭]
    K --> H
    F -->|미등록| L[마케팅 퍼널]
    J --> M{7회 마감?}
    M -->|Yes| N{재등록?}
    N -->|Yes| O[중급반 등 배정]
    N -->|No| P[종료 명단]
    P --> Q[영상 자료 이메일 송부]
```

---

## 2. 예약 → 등록 → 출석 상세

```mermaid
flowchart LR
    subgraph 예약
        R1[네이버 예약]
        R2[예약 목록]
        R3[방문 체크]
    end
    
    subgraph 등록
        E1[등록/미등록/대기]
        E2[반 배정]
        E3[전자계약]
    end
    
    subgraph 출석
        A1[7회차 생성]
        A2[회차별 문자]
        A3[출석 상태 변경]
        A4[양도/환불/상담]
    end
    
    R1 --> R2 --> R3 --> E1 --> E2 --> E3 --> A1 --> A2 --> A3 --> A4
```

---

## 3. 출석 상태 전환

```mermaid
stateDiagram-v2
    [*] --> 예정
    예정 --> 출석: 당일 참석
    예정 --> 양해: 다른 반 이동 등
    예정 --> 조율: 24h 전 연락 (1회 보강)
    예정 --> 결석: 미참석
    예정 --> 비고: 초대/서비스 1회
    note right of 예정: 당일 문자 효력 없음
    note right of 조율: 1회에 한해 보강 가능
```

---

## 4. 회원 숙제·영상 업로드 플로우

```mermaid
flowchart LR
    A[회원 로그인] --> B[회차 선택]
    B --> C[숙제/영상 업로드]
    C --> D[비공개 저장]
    D --> E[강사 확인]
    E --> F[코멘트/체크리스트]
    F --> G[최종 평가 점수]
```

---

## 5. 강사 체크리스트 플로우

```mermaid
flowchart TD
    A[강사 로그인] --> B[담당 회원 목록]
    B --> C[회차 선택]
    C --> D[체크리스트 입력]
    D --> E[수업구조도/비포/애프터/숙제]
    E --> F[개인교감/결석률/재등록률]
    F --> G[최종 평가 10항목]
```

---

## 6. 반 밴드(밴드형) 플로우 — ⚠️ P2 (출시 후 구현)

```mermaid
flowchart LR
    subgraph 강사
        T1[강사 로그인]
        T2[반 밴드 T-011]
        T3[공지·동영상 게시]
        T4[회원 게시물 댓글 피드백]
    end
    
    subgraph 회원
        M1[회원 로그인]
        M2[우리 반 밴드 M-feed]
        M3[숙제·영상 게시]
        M4[강사 댓글 확인]
    end
    
    T1 --> T2 --> T3
    M1 --> M2 --> M3
    T3 --> M2
    M3 --> T2 --> T4 --> M4
```

- **강사**: 반 밴드(홈/게시글/일정/사진첩) → 공지·동영상 게시 → 회원이 올린 숙제·영상 게시물에 댓글(피드백).
- **회원**: 우리 반 밴드(동일 탭 구조) → 숙제·영상 게시 → 강사 댓글(피드백) 확인.

### 6.1 밴드 진입 경로 (설계)

**회원 – 우리 반 밴드(M-feed) 진입**
1. M-001 회원 로그인
2. M-002 회원 홈 진입 (기본 화면)
3. 하단 탭에서 「밴드」 선택 **또는** 홈 카드 「밴드 전체 보기」 클릭
4. M-feed 우리 반 밴드 화면 (홈/게시글/일정/사진첩 탭)

**강사 – 반 밴드(T-011) 진입**
1. T-001 강사 로그인
2. T-002 강사 홈 진입 (기본 화면)
3. 상단 네비에서 「반 밴드」 클릭
4. T-011 반 밴드 화면 (홈/게시글/일정/사진첩 탭)

---

## 7. 1:1 일정 매칭 플로우

```mermaid
flowchart LR
    A[강사 구글 캘린더] --> B[가능 시간 공개]
    B --> C[회원 선택]
    C --> D[회차/일자/목표 입력]
    D --> E[2시간 구성 확정]
```

---

*[[04-화면정의서]]와 동기화. 반 밴드(T-011, M-feed)는 네이버 밴드 스타일 — P2로 분류, MVP에서 제외. 피그마 와이어프레임 작성 시 참조.*
