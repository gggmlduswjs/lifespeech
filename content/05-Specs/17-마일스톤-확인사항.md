---
tags: [lifespeech, milestone, checklist]
updated: 2026-02-22
---

# 마일스톤 & 내가 직접 확인해야 할 것

> Claude가 코드를 짜도 **브라우저·터미널·외부 서비스에서 실제 작동 여부는 내가 직접 확인**해야 함.
> 각 마일스톤에서 아래 체크리스트를 통과해야 다음 단계로 진행.

---

## 🔵 M1 — 1주차 완료 기준 (2/27 금)

> **환경이 전부 연결되었는가?**

### 내가 직접 확인할 것

**Supabase 대시보드 (브라우저)**
- [ ] `supabase.com` 접속 → Table Editor → 9개 테이블 목록 존재 확인
  - users, classes, members, enrollments, attendances, checklists, counselings, evaluations, schedules
- [ ] 각 테이블 컬럼 구조 DB 설계서(`07-DB-설계`)와 일치 확인
- [ ] Authentication → Users 탭 → 3개 테스트 계정 (관리자/강사/회원) 생성 확인

**Railway (백엔드 배포)**
- [ ] `railway.app` → 프로젝트 → Deployments → 빌드 성공(초록) 확인
- [ ] 배포된 URL 복사 → 브라우저에서 `{URL}/health` 접속 → `{ status: "ok" }` 확인

**Postman 직접 테스트**
- [ ] `POST {API_URL}/auth/login` — 관리자 계정 → `access_token` 반환 확인
- [ ] `POST {API_URL}/auth/login` — 강사 계정 → 토큰 반환 확인
- [ ] `POST {API_URL}/auth/login` — 회원 계정 → 토큰 반환 확인
- [ ] `GET {API_URL}/members` (Authorization 헤더 포함) → 200 응답 확인

**❌ 막혔을 때**
- Railway 빌드 실패 → Deployments 탭 로그 캡처 → Claude에게 전달
- Supabase 연결 오류 → `.env`의 `SUPABASE_URL`, `SUPABASE_ANON_KEY` 값 재확인

---

## 🟢 M2 — 2주차 완료 기준 (3/6 금)

> **API 38개가 실제로 작동하는가?**

### 내가 직접 확인할 것

**CSV 업로드 실제 테스트**
- [ ] 네이버 예약 CSV 샘플 파일 준비 (최소 5행)
- [ ] Postman → `POST /reservations/csv-upload` → 파일 첨부 → 업로드
- [ ] Supabase Table Editor → `reservations` 테이블 → 5건 생성 확인

**Supabase Storage 확인**
- [ ] `supabase.com` → Storage → 버킷 목록 존재 확인
- [ ] Postman → `POST /homework/upload` → 테스트 파일 첨부 → Storage에 파일 업로드 확인
- [ ] Storage 브라우저에서 파일 직접 눈으로 확인

**알리고 SMS 실제 발송**
- [ ] `aligo.co.kr` 대시보드 → 잔여 건수 확인
- [ ] Postman → `POST /sms/send` → 내 번호로 테스트 발송
- [ ] **실제 문자 수신 확인** (이게 안 되면 SMS 기능 전체 막힘)

**Railway Swagger**
- [ ] `{API_URL}/docs` 접속 → Swagger UI 표시 확인
- [ ] Swagger에서 직접 로그인 → 토큰 입력 → 아무 GET 엔드포인트 호출 확인

**❌ 막혔을 때**
- SMS 발송 실패 → 알리고 API Key, 발신번호 등록 여부 확인
- Storage 업로드 실패 → Supabase Storage RLS 정책 확인 (Claude에게 정책 수정 요청)

---

## 🟡 M3 — 3주차 D3 · 1차 배포 (3/11 수)

> **소장님이 실제로 접속해서 쓸 수 있는가?**

### 내가 직접 확인할 것

**Vercel 배포 확인**
- [ ] `vercel.com` → 프로젝트 → 빌드 성공(초록) 확인
- [ ] `lifespeech-admin.vercel.app` 브라우저에서 직접 접속
- [ ] 로그인 페이지 표시 확인

**관리자 플로우 직접 테스트 (내가 관리자로 로그인)**
- [ ] 이메일 + 비밀번호 입력 → 대시보드 이동 확인
- [ ] 사이드바 7개 메뉴 모두 클릭 → 각 페이지 이동 확인
- [ ] CSV 파일 드래그앤드롭 → 업로드 → 결과 모달 확인
- [ ] 예약 목록에 업로드된 데이터 표시 확인
- [ ] 예약 1건 상태 변경 → 즉시 Badge 색상 변경 확인

**소장님 계정 확인**
- [ ] 소장님용 관리자 계정 생성 (Supabase Auth에서 직접)
- [ ] **소장님께 URL + 계정 카카오톡 전달**
- [ ] 소장님이 접속 가능한지 확인 (안 되면 즉시 디버깅)

**❌ 막혔을 때**
- Vercel 빌드 실패 → Build Logs 캡처 → Claude에게 전달
- 로그인 후 흰 화면 → 브라우저 F12 → Console 에러 메시지 캡처 → Claude에게 전달
- API 호출 실패 → Network 탭 → 실패한 요청 URL과 응답 코드 확인

---

## 🟡 M4 — 3주차 완료 기준 (3/13 금)

> **3역할 모두 본인 데이터만 보이고, 크로스 동작하는가?**

### 내가 직접 확인할 것

**3역할 로그인 순서대로 테스트 (시크릿 창 3개 열기)**
- [ ] 관리자로 로그인 → 출석부에서 회원 A 1회차 "출석" 처리
- [ ] 강사로 로그인 (같은 브라우저 시크릿) → 출석 체크 화면에서 같은 회원 A 상태 반영됐는지 확인
- [ ] 회원 A로 로그인 → 출석 기록 화면에서 1회차 "출석" 표시 확인
- [ ] **3개 화면이 동일한 데이터를 공유하는지 눈으로 확인**

**역할 격리 확인**
- [ ] 강사 계정으로 `/admin` URL 직접 접속 시도 → 접근 거부 또는 리디렉트 확인
- [ ] 회원 계정으로 `/instructor` URL 직접 접속 시도 → 접근 거부 확인
- [ ] 회원 A로 로그인 → 회원 B의 데이터가 보이지 않는지 확인

**모바일 브라우저 확인**
- [ ] 핸드폰으로 `lifespeech-admin.vercel.app` 접속
- [ ] 로그인 가능한지 확인
- [ ] 주요 화면(예약 목록, 출석부) 레이아웃 깨지지 않는지 확인

**숙제 업로드 확인**
- [ ] 회원 계정 → 숙제 업로드 → 파일 선택 → 업로드
- [ ] Supabase Storage에서 파일 존재 확인
- [ ] 강사 계정 → 해당 회원 숙제 목록에 파일 표시 확인

**❌ 막혔을 때**
- 역할 격리 안 됨 → RLS 정책 오류 → Supabase 대시보드 → SQL Editor에서 정책 확인 후 Claude에게 전달
- 데이터 크로스 안 됨 → Network 탭에서 API 요청/응답 확인

---

## 🟣 M5 — 4주차 AI 기능 (3/17 화) · Whisper STT

> **마이크 녹음이 실제로 작동하고, 텍스트로 변환되는가?**

### 내가 직접 확인할 것

**브라우저 마이크 권한**
- [ ] 크롬에서 `/member/speech-analysis` 접속
- [ ] 마이크 권한 팝업 → "허용" 클릭
- [ ] 녹음 버튼 클릭 → 빨간 표시 + 타이머 작동 확인
- [ ] 30초 녹음 → 중지 → "분석 중..." 표시 확인
- [ ] 실제 텍스트 변환 결과 화면에 표시 확인 (말한 내용이 맞는지)

**OpenAI 대시보드 확인**
- [ ] `platform.openai.com` → Usage → 오늘 Whisper API 사용량 확인
- [ ] 비용이 예상 범위인지 확인 ($0.006/분)

**❌ 막혔을 때**
- 마이크 안 잡힘 → 크롬 설정 → 개인정보 → 마이크 권한 확인
- STT 결과 이상함 → language 파라미터 `ko` 설정됐는지 Claude에게 확인 요청
- API 오류 → Network 탭 → 에러 응답 캡처 → Claude에게 전달

---

## 🟣 M6 — 4주차 AI 기능 (3/18 수) · Claude 스피치 피드백

> **AI 피드백이 실제로 말이 되는가?**

### 내가 직접 확인할 것

**AI 피드백 품질 직접 평가**
- [ ] 1분 스피치 녹음 (실제로 말해보기)
- [ ] AI 피드백 카드 확인:
  - WPM 숫자가 말이 되는지 (적정: 130~150 WPM)
  - 강점이 실제 내용과 관련 있는지
  - 개선점이 구체적이고 유용한지
- [ ] **피드백이 이상하면**: 프롬프트 수정 요청 → Claude에게 개선 방향 전달

**Anthropic 대시보드 확인**
- [ ] `console.anthropic.com` → API Usage → 오늘 사용량 확인
- [ ] 한 번 분석에 토큰이 얼마나 드는지 확인

**❌ 막혔을 때**
- 피드백이 영어로 나옴 → 프롬프트에 "한국어로만 응답" 명시 → Claude에게 수정 요청
- JSON 파싱 오류 → `claude-sonnet-4-6` 응답에서 JSON 이외 텍스트 포함 → 프롬프트에 "JSON만 출력" 강조

---

## 🟣 M7 — 4주차 AI 기능 (3/19 목) · 면접 연습

> **면접 Q&A 사이클이 실제로 작동하는가?**

### 내가 직접 확인할 것

- [ ] `/member/interview` 접속 → 카테고리 4개 버튼 표시 확인
- [ ] "자기소개" 선택 → 질문 1개 표시 확인
- [ ] 실제로 답변 입력 → "AI 평가받기" 클릭
- [ ] 점수(1-10) + 피드백 + 잘한 점 + 개선점 표시 확인
- [ ] 피드백 내용이 구체적이고 유용한지 **직접 읽어보기**
- [ ] "다음 질문" → 다른 질문으로 바뀌는지 확인

---

## 🔴 M8 — 1개월 최종 (3/20 금)

> **소장님이 AI 기능을 직접 써봤고 만족하는가?**

### 소장님 시연 전 최종 점검

**배포 상태**
- [ ] Vercel → 최신 빌드 성공 확인 (배포 시간 오늘인지)
- [ ] Railway → 최신 빌드 성공 확인
- [ ] 소장님이 현재 사이트 접속 가능한지 확인

**AI 기능 최종 점검 (시연 30분 전)**
- [ ] 스피치 분석: 녹음 → 피드백 카드 표시 (30초 테스트)
- [ ] 면접 연습: 질문 → 답변 → 피드백 (1분 테스트)
- [ ] 숙제 피드백: 파일 업로드 → AI 피드백 생성 (2분 대기)

**소장님 시연 중 확인**
- [ ] 소장님이 직접 회원 계정으로 로그인해보기
- [ ] AI 스피치 분석 직접 체험 (소장님이 직접 말하기)
- [ ] 소장님 반응 + 개선 요청사항 메모

---

## 📋 공통 — Claude 개발 시 내가 해야 할 것

> Claude가 코드를 완성했다고 해도 반드시 아래를 직접 확인할 것

| 상황 | 내가 할 것 |
|------|-----------|
| DB 스키마 변경 | Supabase → Table Editor에서 눈으로 확인 |
| 새 API 엔드포인트 | Postman에서 직접 호출 → 응답 확인 |
| 환경변수 추가 | `.env.local` / Railway / Vercel 3곳 모두 설정됐는지 |
| 프론트 화면 완성 | 브라우저에서 직접 열어보기 (코드만 보고 판단 금지) |
| 인증 관련 수정 | 시크릿 창에서 로그인 처음부터 다시 테스트 |
| 파일 업로드 | Supabase Storage에서 실제 파일 존재 확인 |
| 외부 API 연동 | 해당 서비스 대시보드에서 실제 호출 로그 확인 |
| 배포 후 | 배포 URL에서 실제로 접속 → F12 콘솔 에러 없는지 |

---

## 🆘 Claude에게 전달할 때 포함할 것

Claude가 코드를 짠 후 오류가 발생하면 아래 정보를 같이 붙여서 전달:

```
1. 브라우저 F12 → Console 탭 → 빨간 에러 메시지 전체 복사
2. Network 탭 → 실패한 요청 클릭 → Response 탭 내용 복사
3. 터미널 (Railway/Vercel) 빌드 로그 복사
4. 어떤 동작을 했을 때 에러가 났는지 설명
```
