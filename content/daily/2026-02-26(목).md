---
date: 2026-02-26
week: 1주차
phase: 환경 구축
tags: [lifespeech, planner, dev]
---

# 2026-02-26 (목) — 1주차 D4

> **근무:** 10:00 ~ 19:00 · 실 작업 **8시간**
> **목표:** 회원 CRUD + 예약 CRUD + Railway 첫 배포

---

## 시간 블록

| 시간 | 작업 | 예상 | 완료 기준 |
|------|------|------|-----------|
| 10:00–11:30 | 회원 CRUD (GET /members, POST, GET/:id, PATCH) | 1.5h | 4개 엔드포인트 Postman 통과 |
| 11:30–12:00 | 예약 CRUD (GET /reservations, POST) | 0.5h | 2개 엔드포인트 통과 |
| 12:00–13:00 | 🍽️ 점심 | | |
| 13:00–14:00 | PATCH /reservations/:id/status (상태 변경) | 1h | 상태 변경 + DB 반영 |
| 14:00–15:30 | Railway 배포 (GitHub 연동 + 환경변수) | 1.5h | Railway URL /health → 200 |
| 15:30–17:00 | Railway Postman 테스트 (배포 URL) | 1.5h | 배포 환경 3계정 로그인 확인 |
| 17:00–18:30 | 시드 데이터 생성 (회원 5명, 예약 10건) | 1.5h | DB 데이터 확인 |
| 18:30–19:00 | 내일 M1 검증 준비 | 0.5h | 체크리스트 확인 |

---

## 작업 체크리스트

### 회원 CRUD
- [ ] `GET /members` — 페이지네이션 + 이름/전화번호 검색
  - query: page, limit, search
- [ ] `POST /members` — 회원 생성
- [ ] `GET /members/:id` — 상세 + 수강이력
- [ ] `PATCH /members/:id` — 수정

### 예약 CRUD
- [ ] `GET /reservations` — 상태·날짜 범위 필터
  - query: status, from, to, page, limit
- [ ] `POST /reservations` — 예약 생성
- [ ] `PATCH /reservations/:id/status` — 상태 변경
  - pending → confirmed / cancelled / completed

### Railway 배포
- [ ] Railway 프로젝트에 GitHub `lifespeech-api` 연동
- [ ] 환경변수 설정: SUPABASE_URL, SUPABASE_SERVICE_KEY, JWT_SECRET, PORT
- [ ] 빌드 성공 → 초록 확인
- [ ] `{Railway_URL}/health` → `{ status: 'ok' }` 확인

---

## 🤖 Claude 개발 요청 순서

### 요청 1 — 회원 CRUD 라우터
```
회원(members) CRUD API를 만들어줘. src/routes/members.ts

1. GET /members
   - query: page(default 1), limit(default 20), search(이름 또는 전화번호 LIKE 검색)
   - 응답: { data: [...], total, page, limit }

2. POST /members
   - body: { name, phone, email?, memo? }
   - phone 중복 체크 → 409 에러
   - 응답: 201 { id, name, phone, ... }

3. GET /members/:id
   - 회원 정보 + enrollments 조인 (수강이력)
   - 응답: { ...member, enrollments: [...] }

4. PATCH /members/:id
   - body: 수정할 필드만 (partial update)
   - 없는 ID → 404

requireRole('admin') 미들웨어 적용.
```

### 요청 2 — 예약 CRUD 라우터
```
예약(reservations) CRUD API를 만들어줘. src/routes/reservations.ts

1. GET /reservations
   - query: status, from(날짜), to(날짜), page, limit
   - 응답: { data: [...reservations + member info], total, page, limit }

2. POST /reservations
   - body: { member_id, class_id, reserved_date, naver_booking_id? }

3. PATCH /reservations/:id/status
   - body: { status: 'confirmed' | 'cancelled' | 'completed' }
   - 유효하지 않은 상태 → 400

requireRole('admin') 미들웨어 적용.
```

### 요청 3 — Railway 배포 설정 (Dockerfile 또는 nixpacks)
```
Railway 배포를 위한 설정 파일을 만들어줘.

- Dockerfile 또는 railway.toml
- npm run build 후 npm run start 실행
- PORT 환경변수 사용 (Railway가 자동 주입)
- healthcheck 설정 포함
```

---

## ✅ 오늘 내가 직접 확인할 것

**로컬 Postman 테스트**
- [ ] `POST localhost:3000/auth/login` → 관리자 토큰 발급 확인
- [ ] 토큰으로 `GET localhost:3000/members` → 빈 배열 or 목록 확인
- [ ] `POST localhost:3000/members` body: `{ "name": "테스트", "phone": "010-1111-2222" }` → 201 확인
- [ ] `GET localhost:3000/members/{생성된ID}` → 회원 정보 확인
- [ ] `PATCH localhost:3000/reservations/{ID}/status` body: `{ "status": "confirmed" }` → 200 확인

**Railway 배포 확인**
- [ ] `railway.app` → 프로젝트 → Deployments → 빌드 성공(초록) 확인
- [ ] 브라우저 `{Railway_URL}/health` → `{ "status": "ok" }` 확인
- [ ] Postman에서 Railway URL로 로그인 테스트 → 토큰 발급 확인

**❌ 막혔을 때**
- Railway 빌드 실패 → Deployments 탭 로그 캡처 → Claude에게 전달
- 로컬은 되는데 Railway는 안 됨 → 환경변수 누락 확인 (Variables 탭)

---

## 작업 로그

| 시간 | 작업 내용 | 비고 |
|------|-----------|------|
| 10:00 | | |
| 10:30 | | |
| 11:00 | | |
| 11:30 | | |
| 13:00 | | |
| 13:30 | | |
| 14:00 | | |
| 14:30 | | |
| 15:00 | | |
| 15:30 | | |
| 16:00 | | |
| 16:30 | | |
| 17:00 | | |
| 17:30 | | |
| 18:00 | | |
| 18:30 | | |

---

## 메모 / 이슈

-
