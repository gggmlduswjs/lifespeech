---
date: 2026-02-23
week: 1주차
phase: 환경 구축
tags: [lifespeech, planner, dev]
---

# 2026-02-23 (월) — 1주차 D1

> **근무:** 10:00 ~ 19:00 · 실 작업 **8시간**
> **목표:** 외부 서비스 가입 + Fastify 프로젝트 뼈대

---

## 시간 블록

| 시간 | 작업 | 예상 | 완료 기준 |
|------|------|------|-----------|
| 10:00–10:30 | **알리고** 가입 + 발신번호 등록 신청 | 0.5h | 심사 접수 완료 |
| 10:30–11:00 | **Supabase** 프로젝트 생성 + 키 발급 | 0.5h | URL·anon·service key 확보 |
| 11:00–11:30 | **GitHub** 저장소 3개 생성 | 0.5h | 초기 커밋 완료 |
| 11:30–12:00 | **GCP + 모두싸인** 가입 + OAuth ID 발급 | 0.5h | 클라이언트 ID 확보 |
| 12:00–13:00 | 🍽️ 점심 | | |
| 13:00–14:30 | Fastify 프로젝트 초기화 (Claude 요청) | 1.5h | `npm run dev` → 3000 포트 |
| 14:30–16:00 | Supabase 연결 + GET /health 엔드포인트 | 1.5h | `/health` → `{ status: "ok" }` |
| 16:00–17:30 | 글로벌 에러 핸들러 + AppError 클래스 | 1.5h | 없는 라우트 → 404 응답 |
| 17:30–18:30 | Railway 프로젝트 생성 + GitHub 연동 | 1h | Railway URL 확보 |
| 18:30–19:00 | `.env` 정리 + GitHub 커밋 | 0.5h | 초기 커밋 push 완료 |

---

## 작업 체크리스트

### 서비스 가입 (10:00 — 직접 수동)
- [ ] **알리고** 가입 → API KEY 발급 → **발신번호 등록 신청** (⚠️ 심사 3~5일 소요)
- [ ] **Supabase** 프로젝트 생성 (리전: ap-northeast-1, 프로젝트명: lifespeech)
  - URL · anon key · service key → 메모장에 저장
- [ ] **GitHub** 저장소 3개 생성
  - `lifespeech-api` (Fastify 백엔드)
  - `lifespeech-admin` (Next.js 관리자 웹)
  - `lifespeech-app` (Expo 모바일, 5주차~)
  - 각 repo: README + .gitignore + 초기 커밋
- [ ] **GCP** 프로젝트 → Calendar API 활성화 → OAuth 클라이언트 ID/Secret 발급
- [ ] **모두싸인** 가입 → API KEY 발급
- [ ] **소장님 구글 계정 접근 승인** 요청 (카카오톡)

### Fastify 초기화 (13:00 — Claude 요청)
- [ ] TypeScript + nodemon + ESLint + Prettier 설정
- [ ] 디렉토리 구조: `src/routes/`, `services/`, `middlewares/`, `utils/`, `types/`
- [ ] `.env.example` 작성 + `.env` 파일 생성
- [ ] `GET /health` → `{ status: 'ok', timestamp, db: 'connected' }`
- [ ] 글로벌 에러 핸들러 + AppError 클래스 + 표준 응답 포맷

---

## 🤖 Claude 개발 요청 순서

### 요청 1 — Fastify 프로젝트 초기화
```
lifespeech-api Fastify 백엔드 프로젝트를 초기화해줘.

조건:
- TypeScript + nodemon + ESLint + Prettier
- 디렉토리: src/routes/, services/, middlewares/, utils/, types/
- package.json scripts: "dev": "nodemon src/index.ts", "build", "start"
- .env.example (SUPABASE_URL, SUPABASE_SERVICE_KEY, JWT_SECRET, PORT=3000)

결과물: src/index.ts, tsconfig.json, package.json, .env.example
```

### 요청 2 — Supabase 연결 + Health 엔드포인트
```
Supabase 클라이언트를 초기화하고 GET /health 엔드포인트를 만들어줘.

응답: { status: 'ok', timestamp: new Date().toISOString(), db: 'connected' }
환경변수: SUPABASE_URL, SUPABASE_SERVICE_KEY
Supabase users 테이블 간단 쿼리로 연결 확인 → 성공 시 db: 'connected', 실패 시 db: 'error'
```

### 요청 3 — 글로벌 에러 핸들러
```
Fastify 글로벌 에러 핸들러를 만들어줘.

- AppError 클래스: constructor(statusCode: number, message: string, code?: string)
- 표준 응답 포맷: { success: false, error: { code, message } }
- setErrorHandler: AppError → statusCode 반환, 나머지 → 500 Internal Server Error
- setNotFoundHandler: { success: false, error: { code: 'NOT_FOUND', message: 'Not Found' } }
```

---

## ✅ 오늘 내가 직접 확인할 것

**서비스 가입 확인**
- [ ] `aligo.co.kr` 로그인 → 마이페이지 → 발신번호 등록 신청 접수 확인
- [ ] `supabase.com` → Dashboard → `lifespeech` 프로젝트 표시 확인
- [ ] Settings → API → URL, anon key, service_role key 3개 복사해서 메모
- [ ] `github.com/{내계정}` → 저장소 3개 (lifespeech-api, lifespeech-admin, lifespeech-app) 확인

**로컬 서버 확인**
- [ ] `npm run dev` 실행 → "Server listening on port 3000" 출력 확인
- [ ] 브라우저 `http://localhost:3000/health` → `{ "status": "ok", "db": "connected" }` 확인
- [ ] 브라우저 `http://localhost:3000/없는경로` → `{ "error": { "code": "NOT_FOUND" } }` 확인

**❌ 막혔을 때**
- db: 'error' → .env 파일 SUPABASE_URL, SUPABASE_SERVICE_KEY 값 다시 확인
- nodemon 오류 → `npm install` 재실행

---

## 작업 로그

| 시간 | 작업 내용 | 비고 |
|------|-----------|------|
| 10:00 | | |
| 10:30 | | |
| 11:00 | | |
| 11:30 | | |
| 13:00 | | |
| 13:30 | | |
| 14:00 | | |
| 14:30 | | |
| 15:00 | | |
| 16:00 | | |
| 17:00 | | |
| 17:30 | | |
| 18:00 | | |
| 18:30 | | |

---

## 메모 / 이슈

-

---

## 내일 준비

- 07-DB-설계 문서 열어두기 (9개 테이블 SQL 작성 기준)
- 내일(화) 13시 출근 → DB 작업 집중
